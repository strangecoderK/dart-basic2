// `1부터 39`번 사이의 랜덤한 숫자를 생성합니다.
// `Collection` 타입 중 하나를 사용해서 5개의 숫자를 저장합니다.
// - **'숫자들은 모두 겹치지 않아야'** 하며 `5`개의 숫자를 저장한 타입을 반환합니다.
//
// 코드는 작성하지 않고 직접 그림을 그려도 좋고 툴을 사용해서 그려도 좋습니다.
// 순서도를 코드로 옮기면 그대로 동작할 수 있도록 그림을 그립니다.

import 'dart:math';

void main() {
  Set<int> numbers = {};



}

number(){
  return Random().nextInt(40);
}



